server:
  port: 8081

#logging:
#  level:
#    org.springframework: DEBUG

spring:
  messages:
    basename: messages
#  mvc:
#    pathmatch:
#      matching-strategy: ant_path_matcher
#  security:
#    user:
#      name: test
#      password: pwd
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    show-sql: true
  h2:
    console:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "*"